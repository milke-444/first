1. tomcat如何修改端口
   --- 我们可能直接会回答在配置文件中修改port,但实际上需要申明在application中的server下添加port:输入要修改的端口号。把server带上，让其更好理解 在application中的server下添加port:输入要修改的端口号。
3. try catch finally 异常处理：finally字段不管异常有无都会在try后执行。finally中会返回一些返回值之类的。
4. jwt:tonken存储一些用户的信息，避免使用sesson会话去存储一些信息。这里要重点申明服务端信息存储在了token中。
5. 类和抽象类和接口的区别：1. 普通类就是可以被new出来，必须写确定的方法 2.抽象类的话，不能通过new去创建，然后可以有方法。3. 接口的化：不能创建，他只是定义了一种规范，具体功能的实现需要对应的实现类。
6. ssh协议的默认端口是：22
7. 异常的三种类型：首检异常，运行时异常，error
8. mybatis如何操作数据库：
