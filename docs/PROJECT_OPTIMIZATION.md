阶段一：结构工程化（预计 2-3 天）
序号	任务	状态	下一步具体操作
1	包名规范化修正	✅ 完成	—
2	类名与变量命名规范化	✅ 完成	—
3	分包结构调整（entity/vo/dto）	✅ 完成	—
4	全局统一返回格式	✅ 完成	—
5	全局异常处理器	⚠️ 部分完成	补充业务异常（如：博客不存在、权限不足、评论重复）
6	接口参数校验注解补全	✅ 完成	给 LoginDto、BlogDto 等加 @NotBlank、@NotNull
7	ThreadLocal 内存泄漏修复	✅ 完成拦截器 afterCompletion 中加 BaseContext.remove()
阶段二：内容分类体系搭建（预计 1-2 天）
序号	任务	状态	下一步具体操作
1	文章表加 type 和 tag 字段	⬜	ALTER TABLE article ADD COLUMN type VARCHAR(20)
2	文章发布接口支持选择类型	⬜	接口加 type 参数，存库
3	文章列表接口支持按类型/标签筛选	⬜	查询加 WHERE type = ? 条件
4	前端展示支持 Tab 切换	⬜	三个 Tab：笔记/刷题/面经
阶段三：AI 功能集成（预计 2-3 天）
序号	任务	状态
1	DeepSeek API 接入与 Service 封装	✅
2	短视频文案生成接口	⬜
3	文章摘要生成接口	⬜
4	标题优化建议接口	⬜
5	AI 生成历史记录管理	⬜
阶段四：个人工作台功能（预计 1-2 天）
序号	任务	状态
1	Markdown 编辑与渲染	⬜
2	全站内容搜索	⬜
3	个人学习数据看板	⬜
4	收藏与稍后阅读	⬜
阶段五：工程化与部署（预计 1-2 天）
序号	任务	状态
1	Docker 容器化配置	⬜
2	Docker Compose 一键部署	⬜
3	Knife4j API 文档	⬜
4	README 与架构图重写	⬜
阶段六：测试与优化（贯穿始终）
序号	任务	状态
1	核心功能单元测试	⬜
2	并发接口压力测试	⬜
3	代码规范检查与清理	⬜
4	浏览器全流程走查	⬜
┌─────────────────────────────────────────────────────┐
│ 第一阶段：修 Bug + 填补空白（1-2 周）                   │
├─────────────────────────────────────────────────────┤
│ 1. 修复 BlogCommentMapper 的两个致命 Bug               │
│ 2. 修复 blogGgeer 拼写                                        │
│ 3. JWT 密钥移到 .env                                          │
│ 4. 给关键写操作加上 @Transactional(rollbackFor=Exception.class) │
│ 5. 用 BCryptPasswordEncoder 替换 MD5                         │
│ 6. SA-Token启用，补齐权限体系                               │
└─────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│ 第二阶段：补测试，建质量防线（1-2 周）                    │
├─────────────────────────────────────────────────────┤
│ 1. 学习 JUnit 5 + Mockito，给 BlogService 写单元测试       │
│ 2. 给点赞 Lua 脚本、排行榜、评论树写集成测试                    │
│ 3. 集成 Knife4j，生成 API 文档                                │
│ 4. 学会看测试覆盖率报告（JaCoCo）                            │
│ → 面试价值：展示你有质量意识，知道怎么保证代码可靠              │
└─────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│ 第三阶段：利用已有依赖完成功能（2-3 周）                   │
├─────────────────────────────────────────────────────┤
│ 1. DeepSeek AI：实现 AI 写摘要、AI 标题建议                  │
│ 2. RabbitMQ：点赞数据用消息队列异步同步，解耦定时任务            │
│ 3. 补全评论管理：后台审核、评论删除功能                       │
│ → 面试价值：证明你不是"导了依赖但不落地"的开发者               │
└─────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│ 第四阶段：性能优化（2-3 周）                              │
├─────────────────────────────────────────────────────┤
│ 1. 评论树的 N+1 → 一次查出全部评论、Java 中组树              │
│ 2. 博客列表页加 Redis 缓存 + 缓存预热 + 缓存失效策略           │
│ 3. SQL 加 EXPLAIN 分析，建复合索引                           │
│ 4. 接入 Redis 限流（令牌桶或滑动窗口）                        │
│ 5. 热点博客标题用本地缓存 Caffeine 兜底                       │
│ → 面试价值：突出表现你对"高并发、高性能"有实战理解              │
└─────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│ 第五阶段：工程化 + DevOps（1-2 周）                        │
├─────────────────────────────────────────────────────┤
│ 1. 写 Dockerfile + docker-compose（MySQL + Redis + 应用）    │
│ 2. 配置 GitHub Actions 自动构建                              │
│ 3. 区分 application-dev.yml / application-prod.yml           │
│ 4. 接入日志框架（Logback 输出到文件），学会排查线上问题          │
│ → 面试价值：全栈工程化能力，拉开与普通学生的差距                  │
└─────────────────────────────────────────────────────┘
