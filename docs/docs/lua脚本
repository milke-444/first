## Lua 脚本与原子性

### 为什么会有非原子问题？

Java 是天生多线程的，Tomcat 为每个请求分配一个线程。当同一用户在高并发下发起多个请求时，
这些请求被不同线程处理，轮流获得 CPU 执行权，导致 Redis 命令的执行顺序不可预测。

以点赞功能为例，业务逻辑包含三个步骤：
1. SISMEMBER 判断用户是否点赞
2. SADD/SREM 更新点赞 Set
3. ZADD/ZREM 更新排行榜 ZSet

Redis 的单个命令是原子的，但三个命令的组合不是原子的。高并发下可能出现：
- 同一用户多次请求，线程 A 判断用户未点赞 → 线程 B 判断用户未点赞 → 
  两个线程都执行了点赞操作。虽然 Set 的去重机制保证用户不会重复存储，
  但本该取消的请求错误地走了点赞分支，导致最终 Set 中的人数与预期不符。
- 导致点赞数、点赞状态、排行榜数据不一致。

### Lua 如何解决？

将三个步骤写入一个 Lua 脚本，Redis 单线程执行整个脚本，执行期间不会处理任何其他命令，
从而保证"判断 + Set 操作 + ZSet 操作"的整体原子性。
本项目通过 Lua 脚本将 Set 与 ZSet 的操作打包为一个原子事务，一次性发送给 Redis 执行。

### 总结

Lua 脚本主要保证数据从判断到操作 Redis 这一整个步骤是原子的，从而避免由于
条件判断带来的竞态和多个 Redis 命令组合产生的非原子性问题。
